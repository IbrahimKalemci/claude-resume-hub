# docs

`dashboard.svg` is a preview of the `--web` live dashboard, shown in the main README.

Want a real screenshot instead? Run:

```bash
npx claude-resume-hub --web
```

Open the dashboard while it's in the "waiting" state (big countdown showing), take a
screenshot, save it as `docs/dashboard.png`, and point the README `<img>` at it.
